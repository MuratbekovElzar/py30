[tool.poetry]
name = "project"
version = "0.1.0"
description = "hello"
authors = ["Usenovaa <usenova.a00@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
